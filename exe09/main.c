#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "functions.h"

int main() {
  int numa=0;
  int numb=0;
  char placar[100];
  scanf("%d",&numa);
  scanf("%d",&numb);
  placas(placar,numa,numb,0);
}